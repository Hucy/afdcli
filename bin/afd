#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', 'generate a new project from a template')
  .command('dev', 'dev environment')
  .command('pre', 'pre environment')
  .command('dep','deploy')
  .parse(process.argv)
